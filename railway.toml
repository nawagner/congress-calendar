[build]
builder = "nixpacks"

[deploy]
startCommand = "uvicorn congress_calendar.app:create_app --host 0.0.0.0 --port ${PORT:-8000} --factory --proxy-headers --forwarded-allow-ips='*'"
healthcheckPath = "/health"
healthcheckTimeout = 30
